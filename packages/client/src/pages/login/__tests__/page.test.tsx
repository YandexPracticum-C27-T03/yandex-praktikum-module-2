// import { render, screen } from '@testing-library/react';
// import { LoginPage } from '../ui/Page';

// const pageContent = 'Login';

test('renders correctly', async () => {
  // render(<LoginPage />);
  // expect(screen.getByText(pageContent)).toBeDefined();
});
